/**
 * Every page fills the whole content area — no max width, no side gutters — so
 * navigating between them never shifts the layout. The only horizontal spacing
 * is the padding on `<main>` in the root layout.
 */
export const HUD_PAGE_WIDTH = 'max-w-none';
